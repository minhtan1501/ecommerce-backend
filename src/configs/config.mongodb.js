'use strict';

const config = {
    app: {
        port : 3000,
    },
    db :{
        name: "@cluster0.pf8x2mq.mongodb.net/",
        port: 27017,
        userName: "baocao",
        password: "S0qefw0nn.",
    }
}

module. exports = config